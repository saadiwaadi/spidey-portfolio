{
  "name": "portfolio-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix server",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "http-server": "^14.1.1"
  }
}
